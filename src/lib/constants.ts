// ── Shared Application Constants ──
// Centralized constants to avoid magic numbers scattered across files.

/** Maximum number of downloads allowed per order */
export const MAX_DOWNLOADS = 15;

/** Threshold at which download quota shows a warning (remaining count) */
export const DOWNLOAD_WARNING_THRESHOLD = 5;
